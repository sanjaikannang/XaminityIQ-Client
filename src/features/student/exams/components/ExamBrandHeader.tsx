// Small brand wordmark shown on distraction-free exam-flow pages (pre-flight
// check, exam room) which don't render the app's normal sidebar/header shell.
const ExamBrandHeader = () => (
    <span className="font-bold text-lg text-primary tracking-tight shrink-0">XaminityIQ</span>
);

export default ExamBrandHeader;
